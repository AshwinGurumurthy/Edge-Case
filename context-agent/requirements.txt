# Core graph + models
langgraph>=0.2.0
langchain-core>=0.3.0
pydantic>=2.7
requests>=2.31.0          # local Ollama backend (/api/chat) + scanner
python-dotenv>=1.0.0
typing-extensions>=4.0.0

# Cloud backend (used when ANTHROPIC_API_KEY is set; imported lazily otherwise)
anthropic>=0.49
langchain-anthropic>=0.3.0

# Optional web-search grounding for the scenario agent (used when TAVILY_API_KEY is set)
langchain-tavily>=0.2.0
langchain-community>=0.3.0
tavily-python>=0.5.0
